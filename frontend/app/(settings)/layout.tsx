export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return <main className="page-enter mx-auto min-h-screen w-full max-w-2xl px-4 pb-32 pt-5 sm:px-6">{children}</main>;
}
